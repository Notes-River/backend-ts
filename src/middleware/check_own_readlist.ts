import {Request, Response, NextFunction} from 'express';

// export const checkOwnReadlist = (req:Request, res:Response, next:NextFunction) => {
//     if(req.user.readList.includes(req.params.id)){
//         next();
//     }
// }